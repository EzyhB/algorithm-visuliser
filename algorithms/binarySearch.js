let arr = [243, 89, 43, 484, 322, 412, 81, 43, 338, 44];

export var binarySearch = function(array, target) {
    let right = array.length - 1;
    let left = 0;

    while (left <= right) {
        let mid = parseInt((right - low) / 2);
    }
};